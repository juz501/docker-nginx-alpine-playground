const assert = require('assert');
describe('One', function() {
   describe('#equalsOne()', function() {
        it('should be equal to one', function(done) {
            assert.equal(1, 1);
            done();
        });
    });
});